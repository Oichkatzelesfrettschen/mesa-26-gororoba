# Maintainer: Terascale Functionalists
#
# mesa-gororoba: a side-by-side Mesa build from the mesa-26-gororoba fork.  It
# ships the r300 Gallium GL driver, the llvmpipe software fallback, and the
# amd_r300 (r300vk) Vulkan ICD under /opt/mesa-26-gororoba so stock mesa can stay
# installed for A/B comparisons.  Use mesa-26-gororoba-run for loader-visible
# probes such as glxinfo, eglinfo, glesinfo, vainfo, and vulkaninfo.
#
# Scope is deliberately RS482-focused: r300 hardware GL + llvmpipe fallback +
# the r300vk Vulkan ICD.  radv (vulkan-radeon) is inert on pre-GCN RS482 and
# stays a separate stock package; OpenCL (rusticl) is not built.  Build for the
# x86-64 psABI baseline so the artifact runs on the K8 Turion 64 X2 in the
# Vostro (no SSE4.2/AVX).  llvm major MUST match the target host's llvm-libs
# (built and run against llvm 22): a major mismatch makes every llvmpipe path
# fail to dlopen libLLVM.so.<major>.  The .install scriptlet warns on mismatch.
#
# Two lanes, do not confuse them:
#   * DEV / rapid iteration: build the devenv tree (ninja -C <builddir>) and run
#     via VK_DRIVER_FILES (Vulkan) or LIBGL_DRIVERS_PATH+LD_LIBRARY_PATH (GL).
#     Incremental, seconds per file.  This package is NOT for per-file iteration.
#   * RELEASE / proper install: this PKGBUILD -> /opt/mesa-26-gororoba.  Full
#     build (~4 min on a fast x86-64 host).  Promote here once a change is
#     stable.
#
# Install / reinstall:
#   sudo pacman -U mesa-gororoba-*.pkg.tar.zst
# Run probes without changing the system default:
#   mesa-26-gororoba-run glxinfo -B
#   mesa-26-gororoba-run eglinfo
#   mesa-26-gororoba-run vainfo
#   mesa-26-gororoba-run vulkaninfo --summary

pkgname=mesa-gororoba
pkgver=26.2.0
pkgrel=4
epoch=2
# Pinned to the merged-main tip.  Update per build; the source is the public
# fork, not a local path.  For rapid iteration override with #branch=main and
# rebuild.
_commit=102dbd859f97a1abdcde59704f7fed08091f1d50
pkgdesc='Mesa side-by-side /opt build: r300 GL + llvmpipe + amd_r300/r300vk Vulkan ICD, x86-64 baseline'
arch=('x86_64')
url='https://github.com/Oichkatzelesfrettschen/mesa-26-gororoba'
license=('MIT')

depends=(
  expat glibc libdrm libelf libgcc libglvnd libstdc++ libx11 libxcb libxext
  libxdamage libxshmfence libxxf86vm libva llvm-libs lm_sensors spirv-tools
  wayland zlib zstd vulkan-icd-loader
)
makedepends=(
  clang llvm meson ninja glslang python-mako python-ply git
  wayland-protocols libxrandr xorgproto libxml2 lld
)
# Parallel /opt install: no provides/conflicts/replaces.  Stock mesa remains the
# system default and this package is selected explicitly through wrapper/env
# files.  That keeps comparison probes reproducible and avoids pacman conflicts
# on normal system upgrades.
options=('!lto' '!debug' 'strip')
install="${pkgname}.install"
source=("${pkgname}::git+https://github.com/Oichkatzelesfrettschen/mesa-26-gororoba.git#commit=${_commit}"
        'mesa-26-gororoba-env.sh'
        'mesa-26-gororoba-run')
sha256sums=('SKIP'
            'b20eb01633d61e3a1a3d4efd8edf66409b93a0e439718acf294c1ee4f3e7af7e'
            '1aba100cad006b67b31be2c5c31814e25a58d74c320d556678136bef726138c9')

_prefix=/opt/mesa-26-gororoba

build() {
  # clang + lld toolchain.
  export CC=clang
  export CXX=clang++
  export CC_LD=lld
  export CXX_LD=lld

  # x86-64 psABI baseline so the artifact runs on the K8 Turion (no SSE4.2/AVX).
  # Exported here so they override any host-native -march the build host's
  # makepkg.conf injects; meson folds CFLAGS/CXXFLAGS/LDFLAGS into its arg base.
  # Keep the default machine-neutral: no -pipe, no LTO, no -fno-plt, no
  # native tune.  target-cpu=x86-64 guards any Rust translation unit from
  # emitting K8-incompatible code.
  export CPPFLAGS='-D_FORTIFY_SOURCE=2'
  export CFLAGS='-march=x86-64 -mtune=generic -O2 -fexceptions -fstack-protector-strong -fstack-clash-protection -fcf-protection -Wformat -Werror=format-security'
  export CXXFLAGS="${CFLAGS} -Wp,-D_GLIBCXX_ASSERTIONS"
  export LDFLAGS='-Wl,-O1 -Wl,--sort-common -Wl,--as-needed -Wl,-z,relro'
  export RUSTFLAGS='-C target-cpu=x86-64'

  meson setup "${pkgname}" build \
    --prefix="${_prefix}" \
    --sysconfdir=/etc \
    --buildtype=release \
    --wrap-mode=nofallback \
    -D b_ndebug=true \
    -D b_lto=false \
    -D platforms=x11,wayland \
    -D gallium-drivers=r300,llvmpipe \
    -D vulkan-drivers=amd_r300 \
    -D r300vk-gallium-backend=true \
    -D gallium-rusticl=false \
    -D llvm=enabled \
    -D draw-use-llvm=true \
    -D egl=enabled \
    -D gbm=enabled \
    -D glx=dri \
    -D gles1=enabled \
    -D gles2=enabled \
    -D opengl=true \
    -D glvnd=enabled \
    -D gallium-va=enabled \
    -D video-codecs=mpeg12dec \
    -D valgrind=disabled \
    -D libunwind=disabled

  meson compile -C build
}

package() {
  meson install -C build --destdir "${pkgdir}"

  install -Dm755 mesa-26-gororoba-run \
    "${pkgdir}/usr/bin/mesa-26-gororoba-run"
  install -Dm644 mesa-26-gororoba-env.sh \
    "${pkgdir}${_prefix}/share/mesa-26-gororoba/mesa-26-gororoba-env.sh"
  install -Dm644 mesa-26-gororoba-env.sh \
    "${pkgdir}/etc/opt/mesa-26-gororoba/mesa-26-gororoba-env.sh"

  # Compatibility aliases for older build-infra and local scripts.  The
  # canonical prefix is the FHS-style /opt/mesa-26-gororoba; /opt/local was the
  # build-infra staging prefix and /opt/share was used by an older debug package.
  install -dm755 "${pkgdir}/opt/local" "${pkgdir}/opt/share" "${pkgdir}/usr/share"
  ln -s /opt/mesa-26-gororoba \
    "${pkgdir}/opt/local/mesa-26-gororoba"
  ln -s /opt/mesa-26-gororoba \
    "${pkgdir}/opt/share/mesa-26-gororoba"
  ln -s /opt/mesa-26-gororoba/share/mesa-26-gororoba \
    "${pkgdir}/usr/share/mesa-26-gororoba"

  # Mesa ships its license summary as docs/license.rst (Arch/CachyOS practice).
  # A missing license is a packaging defect, not something to swallow silently.
  if [ -f "${pkgname}/docs/license.rst" ]; then
    install -Dm644 "${pkgname}/docs/license.rst" \
      "${pkgdir}/usr/share/licenses/${pkgname}/license.rst"
  else
    echo "WARNING: ${pkgname}/docs/license.rst not found; license not installed"
  fi
}
