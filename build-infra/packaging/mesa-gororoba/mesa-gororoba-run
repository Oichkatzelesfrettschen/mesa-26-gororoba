#!/bin/sh
# Select the package-owned R3V manifest for one command.
set -eu
if [ "$#" -eq 0 ]; then
  echo 'usage: mesa-gororoba-run <command> [args...]' >&2
  exit 2
fi
# shellcheck disable=SC1090
. "${MESA_ENV_FILE:-/etc/mesa-gororoba/mesa-gororoba-env.sh}"
exec "$@"
