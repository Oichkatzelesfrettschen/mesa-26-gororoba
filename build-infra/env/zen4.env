# zen4.env -- AMD Ryzen Zen 4 native env for high-throughput builds.
# Placeholder -- fill in when the Ryzen workstation joins the workspace.

export CC="${CC:-clang}"
export CXX="${CXX:-clang++}"
export CFLAGS="-march=znver4 -mtune=znver4 -O2 -pipe"
export CXXFLAGS="$CFLAGS"
